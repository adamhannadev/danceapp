<%= form_with(model: dance_category) do |form| %>
  <% if dance_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(dance_category.errors.count, "error") %> prohibited this dance_category from being saved:</h2>

      <ul>
        <% dance_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
